0. Need to log into machines in test/hosts for use python3

1. Start License server:
   in SclApis directory, invoke like:
   ./LmServer.py --host fsei317 --port 50008

2. set enviroment variable for license server:
   setenv LM_SERVER fsei317@50008

3. in tests directory, run like follows:
   3.1 run with json file directory like:
       ../CustomFault/CustomFault -j testDVP.json
   3.2 run for gui
       ./CustomFault/CustomFault -g 1 
   